services:
  - type: worker
    name: freelance-job-alerts-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python job_alerts.py
    autoDeploy: true
